# Frozen common contract

DeepSeek receives only the per-run files listed by the local controller. No web,
database, repository-search, shell, Python-execution, or server-side tool is
enabled. The structural facts are computed by the original frozen pure-Python
preparation pipeline and passed through `model_features.json`.
